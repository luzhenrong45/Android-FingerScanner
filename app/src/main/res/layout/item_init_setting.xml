<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="20dp"
    android:clickable="true"
    android:background="@drawable/item_init_setting_bg">

    <TextView
        android:id="@+id/item_left_tip"
        android:layout_width="30dp"
        android:layout_height="60dp"
        android:paddingLeft="5dp"
        android:paddingTop="3dp"
        android:text="1"
        android:textColor="@android:color/white" />

    <TextView
        android:id="@+id/item_right_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginLeft="6dp"
        android:layout_marginTop="10dp"
        android:layout_toRightOf="@id/item_left_tip"
        android:text="允许显示悬浮窗"
        android:textSize="20sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/item_right_msg"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@id/item_right_title"
        android:layout_below="@id/item_right_title"
        android:layout_marginBottom="10dp"
        android:layout_marginTop="4dp"
        android:text="确保「指纹解锁」可以正常运行" />

</RelativeLayout>
